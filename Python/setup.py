from setuptools import setup, find_packages

setup(
    name="ilinkai-weixin-sdk",
    version="1.0.0",
    description="ILinkai微信SDK Python版",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
